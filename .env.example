# 环境变量示例。复制成 .env 后按需修改（`.env` 在 .gitignore 里，不会进版本控制）。
#
# 本项目的**必需**环境变量只有一个：SAVES_ENABLED。其余都有合理默认值。

# ⚠️ 存档端点（/api/saves）的硬开关。默认**关**。
#
# 关掉时应用仍然能跑，但数据只活在这个浏览器里 —— 换个浏览器、清一次缓存就没了。
# 自建部署（Docker / node server.mjs）应当打开它。
#
# 反过来说：**打开它就等于把所有人的数据暴露在同一个 HTTP 面下**。部署到公网之前
# 必须先加一层认证（反向代理的 basic-auth 即可），见 README「部署」。
SAVES_ENABLED=1

# 存档目录的**父目录**，存档落在 <它>/saves/<userId>/。默认是进程的工作目录。
# 容器里指向挂载卷；本机跑可以不设。
SAVES_ROOT=/data

# 部署在反向代理后面时设为 1。否则所有请求会被当成同一个来源、共用一个限流额度
# （现象是「用着用着就 429」）。
#
# ⚠️ 直接暴露端口时**不要设**：那种情况下转发头完全由客户端伪造，采信它等于没有限流。
TRUST_PROXY=1

# 监听端口与地址。默认 3000 / 0.0.0.0。
# 只在本机用时建议把 HOSTNAME 设成 127.0.0.1。
# PORT=3000
# HOSTNAME=127.0.0.1

# 站点对外地址，构建期注入（用于 og:image 之类的绝对 URL）。不设则不输出绝对 URL。
# SITE_URL=https://example.com
