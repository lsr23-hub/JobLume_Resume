# JobLume Resume — 项目文档

个人职业数据库 + JD 智能匹配 + 简历自动生成工具。基于 [Magic Resume](https://github.com/JOYCEQL/magic-resume) v2.0.8 二次开发。

> `rawproject/` 为上游代码的只读参考副本，不参与开发，不纳入版本管理。

---

## 文档索引

| 文档 | 内容 | 适用场景 |
|---|---|---|
| [01-PRD.md](./01-PRD.md) | 产品定位、用户场景、功能需求、优先级 | 理解「做什么、为什么做」 |
| [02-data-model.md](./02-data-model.md) | 数据结构、物化层、store 设计、数据流 | 理解「数据怎么组织」 |
| [03-generation-algorithm.md](./03-generation-algorithm.md) | 通用排序（时效 × 同类衰减）、LLM 推荐标注、可复现性工程、降级策略 | 理解「怎么排序、怎么匹配、怎么保证稳定」 |
| [04-development-plan.md](./04-development-plan.md) | 阶段划分、文件清单、验收标准、工时 | 理解「怎么做、做多久」 |
| [05-api-and-config.md](./05-api-and-config.md) | DeepSeek 配置、`/api/match` 路由、安全模型、图片存储 | 实现 AI 相关功能时查阅 |
| [06-ai-enablement-map.md](./06-ai-enablement-map.md) | AI 赋能全景：11 个候选点的五层链路、幻觉边界三分法、5 个跨领域决策 | 规划 AI 功能、做总设计时查阅 |

---

## 当前状态

| 项 | 状态 |
|---|---|
| 阶段 | **文档阶段（v3.0）** —— 尚未开始编码 |
| 上游代码 | 已下载至 `rawproject/`，未做任何修改 |
| 开发基线 | 未建立（Phase 0 待执行） |
| Git 仓库 | 未初始化 |

---

## 核心设计一句话

```
职业数据库（唯一事实来源）
      │
      │  无 JD：时效 × 同类衰减排序
      │  有 JD：LLM 标注推荐 + top-N  ◀── 仅此一步依赖 AI
      ▼
用户勾选（唯一决策点）
      │  物化 materialize()
      ▼
简历数据 ResumeData（shape 冻结，9 套模板零改动）
      │  渲染
      ▼
PDF / JSON / Markdown
```

**为什么这样分层**：内容会反复重组，排版只有一套。把变化关在数据层，模板层完全冻结，避免改动 9 套模板 × 8 个 section 组件 = 72 个文件。

---

## 三个产品承诺

1. **事实与呈现分离** —— 数据库存事实，简历存呈现。同一段经历可以生成十份不同侧重的简历，事实只有一份。

2. **AI 是推荐者，不是决策者** —— LLM 只标注「哪些经历值得放进这份简历」，**勾选状态完全由用户产生**，AI 不预设任何默认值。生成路径自始至终是「用户勾选 → 物化」，与 AI 是否可用无关。

3. **判断必须可核对** —— 判为「不推荐」的条目必须逐字引用原文作为依据，引用不出就自动改判为推荐。举证责任在否定一侧：无法举证就不该否定用户的经历。

## 可复现性设计

用户明确要求「匹配结果不要每次相差很远」。四道防线：

| 防线 | 手段 |
|---|---|
| 一 | **二值判断**（推荐/不推荐），档位越少越稳定；LLM 的排序只用于算 top-N，不作为 UI 顺序 |
| 二 | **prompt 逐字节确定**：固定序列化顺序，禁用 `Date.now()` / `Object.keys()` / `toLocaleString` |
| 三 | **采样参数**：`temperature=0` + `seed=42`，由服务端注入，不接受客户端传值 |
| 四 | **指纹缓存**：数据未变时不重跑，结果**零变化**；变更时提示而非自动重跑 |

验收阈值：推荐集合 10 次运行 Jaccard ≥ 0.80，top-N 集合 ≥ 0.70。

## 通用简历的排序

```
最终分 = 时效分 × 0.5^(排在它前面的同类条目数)
```

**为什么不是纯时效**：如果近期经历集中在同一领域，纯时效会让「什么岗位都能投」的简历变成一份领域专用简历。同类衰减确保不同类型的经历都能进入前列 —— 金融 A、CS、运营三条并列前排，金融 B/C 被依次压下。

类别取自条目的第一个标签（`tags[0]`）。λ=0.5 是唯一的经验参数，语义是「同类经历的第二次出现要新一倍才有同等权重」。

**顺序不必事先可预测，但必须事后可验证** —— 界面上每条显示类别与「同类第 N 条」。

---

## 快速导航

**想了解产品要做什么** → [01-PRD.md](./01-PRD.md) §2 用户场景

**想了解数据怎么存** → [02-data-model.md](./02-data-model.md) §3 职业数据库数据模型

**想了解匹配逻辑** → [03-generation-algorithm.md](./03-generation-algorithm.md) §3 LLM 智能匹配

**想知道先做什么** → [04-development-plan.md](./04-development-plan.md) §2 阶段划分

**决策摘要（实现时直接引用）** → [04-development-plan.md](./04-development-plan.md) §12.1

**已关闭的决策与理由** → [01-PRD.md](./01-PRD.md) §7、[02-data-model.md](./02-data-model.md) §9 与 §10
