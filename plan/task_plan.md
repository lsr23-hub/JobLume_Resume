# 主线目标

> 完成一份功能完整的 AI 产品，有完整的 AI 能力量化体系，最后达到能够将该项目写入简历作为我的项目的一部分的水平。

拆成三条**可验收**的线：

| 线 | 验收标准 | 当前 |
|---|---|---|
| **A. AI 能力量化体系** | 口径无已知缺陷；标注集够统计意义；基线可复现；指标能驱动产品决策 | 未收口 |
| **B. 功能完整** | `docs/04 §8.2` 全部勾选，或对做不到的项有明确书面记录 | 8 项未勾 |
| **C. 可讲述** | 一份对外材料：是什么、测了什么、结果如何、已知边界 | 未开始 |

**顺序**：A 与 B 并行 → C。C 必须在 A 收口之后，否则写的数字会变。

---

## A. AI 能力量化体系（核心资产）

### A0. 现状盘点（2026-09-19 核）

| 件 | 状态 | 证据 |
|---|---|---|
| 指标定义与阈值理由 | ✅ | `docs/07-eval-design.md` |
| 评测运行器（mock / real 双 transport） | ✅ | `scripts/eval.ts` |
| 评测报告 | ✅ | `docs/08-eval-report.md`，12 项指标 |
| **口径正确性** | ❌ | `docs/07:190` 自认「未标注案例计 0 分，指标被稀释」，并已写出正确做法 |
| **标注集厚度** | ❌ | 全数据集仅 **15 条**「档案无支撑」标注，一条能让某案例比率动 **25 个百分点** |
| **测量稳定性** | ❌ | 同一份代码连跑两次，「理想集合重合度」实测 **69.3% / 72.1%**，阈值 70% —— 掷硬币 |

**结论：现在的「12 项里 2 项未达标」既不能证明产品有问题，也不能证明没问题。** 这个状态下调 prompt 无法区分「改动有效」与「采样抖动」。

### A1. 修口径 ✅（2026-09-19，提交 `69c1d3f`）
- [x] `覆盖虚报率` 改为在**有标注的案例**上取平均（`docs/07:190` 已载正确做法）
- [x] 逐一核查其余指标 —— **查出一个 docs/07 没记的同款问题**，见下
- [x] 分母打进报告：报告现在写明「覆盖虚报率 3/8、关键经历召回 7/8」
- [ ] `缺失项召回` 的下界性质写进报告正文（`docs/07:184`）—— 报告已自述 v5 口径变更，但下界性质还没写

**核查结果**（逐案例量过分母，不是猜的）：

| 指标 | 分母 | 是否有稀释 |
|---|---|---|
| 覆盖虚报率 | **3/8** | ❌ 原本有 —— 5 个案例注入假满分 |
| 关键经历召回 | **7/8** | ❌ 原本有 —— `jun-02` 一条 must-have 都没标 |
| 缺失项召回 | 8/8 | 无 |
| 要求项召回 | 8/8（每案例 10–15 条要求） | 无 |
| 理想集合重合度 | 恒非空（模型选择非空 → 并集非空） | 无 |
| NDCG / 斯皮尔曼 | 每案例 7–20 条目，n≥2 | 无 |

实现：`index.ts` 新增 `avgAnnotated()`，只在有标注的案例上平均；无合格案例时留
`undefined`，报告渲染成「未采集」—— 「没测」与「测了满分」必须能分开。
新增 4 条测试钉死（298 → 302）。

### A2. 扩标注集
- [ ] 目标：单条标注的影响降到 **< 5 个百分点**（即每案例 ≥ 20 条标注量级）
- [ ] 标注规则写进 `docs/07`，含「模型给出另一个正确但不同的缺口」如何计

### A3. 重新建立基线
- [ ] 重跑，结果写进 `docs/08`
- [ ] **旧数字显式标记为不可比**（口径已变）
- [ ] 记录采样抖动区间：同一份代码跑 ≥ 3 次，报告区间而非单点

### A4. 用尺子驱动一轮真实优化
- [ ] 挑最差的一个指标 → 定位根因 → 改 → 重测
- [ ] 记录**完整闭环**（测出问题 → 诊断 → 修复 → 复测）。**这是简历里最值钱的一段** —— 有失败、有诊断、有修复，比一个漂亮数字可信得多

---

## B. 功能完整

### B0. 现状
`docs/04 §8.2` 最终验收清单有 **8 项未勾**。但其中多数**已经实现**（`backup.ts`、版本管理、工作台都在），只是没人逐项跑过。所以现在说「Phase 5 完成」没有根据 —— `docs/README` 里那句「Phase 0–5 已完成」需按实测结果修正。

### B1. 把 8 项变成可执行验收 —— 部分完成（2026-09-19，提交 `6f57c72`）

新增 `scripts/e2e/acceptance.ts`（`pnpm e2e:acceptance`），**12 项全绿**，连跑两遍稳定。

| `docs/04 §8.2` 的验收项 | 状态 | 由谁验 |
|---|---|---|
| 能生成通用简历并导出 PDF | ⚠️ 部分 | 生成 ✓（`acceptance` ③④）；**导出被文字层缺陷挡着**，见 B2 |
| 数据未变时匹配结果零变化 | ❌ **未验** | 需要一份真实的 `matchAnalysis` 才能验 —— 卡 U2 |
| 未配置 API Key 时手动选择模式可用 | ✅ | `acceptance` ①② |
| 能管理工作台的全部编辑能力 | ⚠️ 部分 | 模板 / 字号 / 板块显隐 ✓（⑤⑥⑦）；**板块排序、字体族未验** |
| 能管理多个简历版本 | ⚠️ 部分 | 复制 / 删除 ✓（⑧⑨⑩）；**按投递目标分组未验** |
| 能全库备份与恢复 | ✅ | 导出 → 清空 → 导入往返（⑫） |
| 未配置 API Key 时核心功能仍可用 | ✅ | `acceptance` ③④ |
| 上游既有功能无回归 | ❌ **未验** | 没有系统性的回归对照；只能靠 `core-flow` 覆盖的主链路 |

**结论**：8 项里 **3 项全验、3 项部分验、2 项未验**。未验的两项都卡在同一个地方
（需要真实 AI 分析），所以 B1 的剩余部分与 A 线的 U2 是同一个阻塞。

补验的成本估计：板块排序 / 字体族 / 投递目标分组 —— 半天内能进 `acceptance.ts`；
「上游无回归」需要先定义回归基线，是个独立决定。

### B2. PDF 文字层缺陷 —— 需重新评估
`docs/04:407` 判为「非本项目可修」，理由：CMap 由 Chromium 打印引擎写出。

**该判断对当前实现成立，但值得重新评估** —— 它是「功能完整」前面唯一一块硬石头。实测：一份简历 160 个汉字里 **18 个（11.3%）** 落在 U+2E80–U+2FDF，如 `页→⻚`、`立→⽴`。字形正常，但复制粘贴与 ATS 解析拿到错字。

- NFKC 能还原大多数，**唯独 `⻚`（U+2EDA）无兼容分解**，还原不了
- 两条现有路径的取舍：「11% 的字读错」（`print.ts` 走浏览器打印，有文字层）vs「全部读不到」（`export.ts` 走 jspdf canvas，无文字层）
- **可能的第三条路**：用 `jspdf` 自绘 PDF 文字层（自带 ToUnicode），绕开 Chromium 的 CMap。代价是复刻模板布局

- [x] **spike 已完成**（2026-09-19）：A/B 对照证明 jsPDF 写出的文字层**零康熙部首**，
      而 Chromium 打印同段文字 11/20 个汉字中招。**「非本项目可修」的判断可以推翻。**
      详见 `plan/notes.md` §七。但有两个条件：jsPDF **只支持 TTF**（6 个 OTF 字体不可用）；
      **版面复刻未验证**，那才是真正的工作量。
- [ ] **据结果定取舍（需要你，U3）**：自绘 PDF / 改文案 / 两条并存
- [ ] 无论走哪条，**当前「文字可选中、可被 ATS 解析」的文案是过度承诺，必须先修**

---

## C. 对外可讲述（等 A 收口后做）

现有素材**已经很强，不要重做**：

- `src/lib/match/buildMatchPrompt.ts` 顶部 v1→v5 版本史，带实测数字（v1 推荐率 79% vs 人工金标准 43%、v3 误判率 45.8%、v4 改「只排序」后 NDCG 88.8%）
- `docs/02-data-model.md` 三条硬约束 + D1–D27 决策表
- 「ponytail 梯子」逐项删除的记录（`docs/03:748` 起）
- `docs/upstream-derivation.md`：从上游 fork 的逐文件归属

- [ ] 一份对外 README/材料：是什么 → 测了什么 → 结果 → 已知边界
- [ ] 把上述决策素材整理成面试可讲的 3–5 个故事

---

## ⏸ 需要你（我无法独立推进的）

按「卡住了谁」排序，不是按重要性。

| # | 事项 | 为什么必须你来 | 阻塞了什么 |
|---|---|---|---|
| U1 | **定标注规则并标注** | 标注是**人工金标准**，它的价值恰恰在于不是模型产出的。我按规则扩可以，规则得你定：一个案例标到什么粒度、同义/近义算不算命中、「另一个正确但不同的缺口」怎么计（`docs/07:181` 已列了三条已知边界，但没定新规则） | A2 → A3 → A4 全链 |
| U2 | **提供 `DEEPSEEK_API_KEY`**（或确认可以花钱跑） | 真实评测要打真实 API。`.env` 现在没有任何 key（我上轮清理时把失效的 `FONTCONFIG_PATH` 换成了说明，没放 key） | A3 重跑基线；A4 验证优化效果 |
| U3 | **B2 的最终取舍** | 半天 spike 我能做，但「自绘 PDF（几周）」vs「改文案说实话（几行）」是产品取舍，得你拍 | B1 第 1 项、B2 |
| U4 | **C 线的受众定位** | 对外材料写给谁决定写什么：HR 筛简历 / 技术面试官 / 学术导师，三者要突出的东西完全不同 | C 全线 |
| U5 | **标注集的规模目标** | 我建议「单条标注影响 <5 个百分点」（约每案例 ≥20 条量级），但这直接决定你要投入多少小时 | A2 的验收标准 |

**U2 补充**：如果你不想在评测上花钱，可以只跑 `--transport mock --mock-mode oracle`（不验证质量，只验证管线）。但那样 A3 只能建立「管线可复现」而非「质量基线」。

---

## 已知边界（诚实记录，不修但要写明）

- **覆盖判定缺口**：校验只查经历存不存在，**不查它与这条要求有没有关系**。所以「精通 Rust 由一段纯前端经历支撑」能通过全部校验，在界面上以「你具备」的姿态出现。已用一条测试钉住，见 `docs/07:132`
- **文档残留**：`docs/03 §3` 正文仍是 v1–v3 旧契约（已加过时横幅）；`suggestedFocus` 是死字段（模型返回、界面不读）
- **开发基建**：`.eslintrc.json` 指向已卸载的 `next/core-web-vitals`，且无 lint script —— lint 完全不可用；`public/fonts` 149MB 每次构建全量复制进 `dist`，其中 2 个文件（37MB）无人引用

---

## 进度

| 日期 | 事项 | 状态 |
|---|---|---|
| 2026-09-19 | 项目独立化完成（移除 rawproject） | ✅ 5 个提交，见 `git log b03737e..b07317d` |
| 2026-09-19 | 主线目标确立，三条线拆解 | ✅ 本文件 |
| 2026-09-19 | A1 修评测口径（+1 个新发现） | ✅ 提交 `69c1d3f`，测试 302 |
| 2026-09-19 | B2 spike：自绘 PDF 文字层可行 | ✅ 提交 `c2eae3b`，待你定取舍（U3） |
| 2026-09-19 | 导出文案去过度承诺 | ✅ 提交 `b9af154` |
| 2026-09-19 | B1 验收清单（12 项全绿，覆盖 3/8） | ✅ 提交 `6f57c72` |
| 2026-09-19 | 模块职责重划：投递目标只做 JD 分析 + 岗位适配度等级 + 内容选择步 | ✅ 9 个提交 `7f9a57b..`；新增 `pnpm e2e:targeted` 15 项 |

---

## 2026-09-19 追加：照片裁剪复用 + 技能分组化 + 荣誉改名

四项用户直接指令，与 A/B/C 主线无关，单独一轮做完。

| # | 事项 | 关键事实 | 状态 |
|---|---|---|---|
| 1 | 简历编辑器「照片」复用职业数据库的 `PhotoCropper` | 编辑器现在**没有裁剪**（`handleFile` 直接压缩存 base64）。`PhotoCropper` 在 `profile/` 下、绑死 `profile` 命名空间 → 提到 `components/shared/` | ✅ 已完成 |
| 2 | 裁剪滑块「没法滑动，只能点击」 | 实测：**Root 命中带只有 8px 高**（track `h-2`），滑块 20px 溢出在外；`−` / `+` 两个图标看着像按钮、实际不可点。Chromium/WebKit 合成拖拽可复现「能拖」，故按**命中区过小 + 无按钮路径**处理 | ✅ 已完成 |
| 3 | 专业技能里证书奖项 / 语言能力改成「分组」形态 | 数据模型不变（`certificateText` / `languageText`），只改面板：两行对齐的分组行，不可拖拽、不可删除（materialize 固定把它们排在最后） | ✅ 已完成 |
| 4 | 板块「荣誉课程」→「获奖情况」 | 只有两处用户可见：`config/sections.ts` 的 `title` 兜底、`zh.json` 的 `profile.sections.honors` | ✅ 已完成 |

**顺带发现**：`PhotoCropper` 的取消按钮用 `t("cancel")` 但 `profile.cancel` 不存在 → 按钮上直接显示英文小写 `cancel`（截图确认）。

### 实际做法与偏差

| 项 | 做法 | 偏差说明 |
|---|---|---|
| 1 | `PhotoCropper` 提到 `components/shared/`，i18n 从 `profile` 命名空间改挂 `photoConfig.crop.*`；编辑器上传后先进裁剪器再落地 | 顺带删掉了 `PhotoConfigDrawer` 里已死的 URL/代理加载逻辑 |
| 2 | 命中带 8px → 24px（Root 加 `py-2`）；`−` / `+` 改成真按钮；加缩放百分比 | **Chromium/WebKit 合成拖拽实测是能拖的**，所以按「命中带过窄 + 没有按钮路径」处理，没有找到能稳定复现的硬故障 |
| 3 | 两个固定行，与分组同形、列对齐；不可拖不可删 | 顺带修：证书/语言的切分原来只认 `\n`，单行输入写不出来 → 改为同时认 `；`/`;` |
| 4 | `config/sections.ts` 的 `title` + `zh.json` 的 `profile.sections.honors` | 英文 `Honors` 本身不误导，未动 |

**顺带清掉的死代码**（上一轮承诺的 + 这一轮新增的孤儿）：
`/api/resume-import`、`/api/proxy/image`、`ResumeWorkbench` 里的 pdfjs 抽图逻辑（约 130 行）、`lib/server/urlGuard.ts` + 其 13 个测试（只服务于已删的 proxy 端点）。

### ⚠️ 新发现的真 bug（未修，需你定方向）

**档案里的照片进不了生成的简历。** `materialize.ts:225` 是 `basic: { ...profile.basic }` 整体铺开，
把档案层的 `idb:img_xxx`（IndexedDB 引用）原样带进了简历层；而 `resolveImageRef` 全仓只在
`profile/BasicPanel.tsx` 被调用过一次。实测：生成的简历 `basic.photo === "idb:img_..."`，
模板渲染时浏览器报 `net::ERR_UNKNOWN_URL_SCHEME` —— **简历上没有照片，导出的 PDF 同样没有**。

修法要选：materialize 变异步、还是把 blob 一并写进简历库、还是在简历层渲染时解析引用。
| 2026-09-19 | 照片裁剪复用 + 技能分组化 + 荣誉改名 + 清死代码 | ✅ 提交见下；新增 `pnpm e2e:photo` 22 项 |

---

## 2026-09-19 追加：多用户（用户为总入口）

完整方案见 `~/.claude/plans/wild-humming-sprout.md`（已按 zustand 源码校验修正）。
四个产品决策：用户=档案 / 分析按「岗位×用户」/ 备份只导当前用户 / 新建切换删除在弹窗。

| 步 | 内容 | 状态 |
|---|---|---|
| 1a | `src/store/userScope.ts` —— 三份持久化状态的迁移 + 归一化 + 按用户读写分析的辅助 | ✅ 21 条单测 |
| 1b | `useCareerProfileStore` 改形状、接 `version: 1` + `migrate` + `merge`、收敛 `put()` 写入漏斗 | ✅ 9 条单测 |
| 2 | 稳定 selector（模块作用域函数）+ `useResolvedImage` | ◐ hook 已建；简历/目标 store 的 selector 未做 |
| 3 | `UserSelectDialog` + `RequireUser` + 侧边栏 chip | ◐ 弹窗与门禁已接；**侧边栏 chip 未做**（暂时只能刷新后靠持久化记忆切人） |
| 4 | 分析读写点切到"当前用户那一份"；删掉 `JobTarget` 上的旧单槽字段 | ⬜ |
| 5 | 备份调用点按用户收口（`lib/backup.ts` 本身不用改） | ⬜ |
| 6 | 6 个既有 e2e 脚本改 localStorage 路径 + 新增 `e2e:users` | ⬜ **当前 4 个既有套件因路径没改而失败** |
| 7 | 文档同步（含已有的 `resume-store` 笔误等存量错误） | ⬜ |

**第 1a 步留下的过渡缝**：`userScope.ts` 导出了 `ScopedJobTarget = JobTarget & TargetScopedAnalysis`。
迁移产出的目标一定带 per-user 分析字段，而 `JobTarget` 接口上旧的两个单槽字段要等第 4 步
全部读取方切完才能删。在那之前，凡"已经过迁移"的目标请用 `ScopedJobTarget` 标注。
