# Task Plan: 代码体检报告

## Goal
出一份**证据驱动**的体检报告，六个维度：无人使用的文件 / 重复功能 / 无用依赖与接口 /
过深调用链 / 缺测试的关键流程 / 安全隐患。
**每条必须给：文件位置 + 问题原因 + 影响范围 + 处理顺序。禁止只给建议。**

## 与前一步的关系
复用 `plan/feature-inventory.md` 与 `plan/feature-interactions.md` 的已核结论（不重复盘点），
**新增**四类测量：规模与复杂度、测试覆盖、安全面、重复实现。

## Phases
- [x] M1: 规模测量（文件行数、嵌套深度、函数长度）
- [x] M2: 测试覆盖测量（哪些模块有测试、哪些没有；关键流程有没有 e2e 覆盖）
- [x] M3: 安全面测量（密钥存储、鉴权、CORS、注入面、泄露面）
- [x] M4: 重复实现测量（4 组，含 2 组已漂移）
- [x] M5: 调用链深度测量（5 条链，跨界 3–8 层；另挖出 4 个持久化缺陷）
- [x] M6: 汇总成 `plan/health-report.md`，按处理顺序排（9 节）

## Ground Rules
- 只读，不改源码
- 每条**必须**四要素齐全，缺一不算完成
- 量化优先：给数字，不给「较多」「偏深」
- 与上一步重复的结论要引用而非重写

## Status
**完成。** 报告 8 节，P0 条目 5 个、P1 条目 12 个。未改动任何源码。

## 交付物
- `plan/health-report.md` —— 体检报告（8 节，含 P0–P3 处理顺序总表）
- `scripts/audit/xss-proof.mjs` —— XSS 载荷验证脚本（7/7 存活，可重跑）

## 本次新增的测量（前一份清单没有的）
| 测量 | 结果 |
|---|---|
| 文件规模 | 超 400 行 13 个，超 800 行 1 个（`useResumeStore.ts` 932 行）|
| 函数规模 | 单个函数超 80 行者 **70 个**，最大 752 行 |
| 嵌套深度 | 逻辑嵌套 >10 仅 2 处，且都在死代码/装饰组件里 → **不是问题** |
| 测试覆盖 | `src/lib/**` 18/37；组件测试 **0**；`analyzeMatch` 与服务端三文件无测试 |
| 安全 | XSS **实测 7/7 载荷存活**；GitHub token 随导出外泄；无 CSP |
| 调用链 | 5 条链跨界 3–8 层；`journal.ts` 有实现有测试但**从未接线** |
| 类型检查范围 | `tsconfig.json` 不含 `scripts/` → 9 个 e2e 脚本编译期零检查 |
